# npm audit — 2026-08-31

No patch/minor upgrades available this week (all outdated packages require major-version bumps).

## Security advisories (HIGH)

| Package | Finding |
|---|---|
| `next` | Multiple HIGH advisories (9.3.4-canary.0 – 16.3.0-preview.10 affected) — fixed in ≥ 16.3.0 (major bump needed) |
| `sharp` | Inherited libvips vulnerabilities — fixed in `sharp ≥ 0.35.0` (minor bump on 0.x, potentially breaking) |
| `postcss` | XSS via `</style>`; path traversal via source maps |
| `nanoid` | Non-secure/custom generators loop indefinitely |
| `undici` | Insufficient randomness; unbounded decompression chain |
| `brace-expansion` | DoS via exponential expansion |
| `nodemailer` | Vulnerability in versions ≤ 9.0.0 |
| `socket.io-parser` | Vulnerability in 4.0.0 – 4.2.6 |
| `fast-uri` | Vulnerability in 3.0.0 – 3.1.4 |
| `linkify-it` | Vulnerability in ≤ 5.0.1 |

All fixes require major-version bumps (out of scope for this automated run). Manual review recommended.
